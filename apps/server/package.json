{
  "name": "server",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --watch src/index.js",
    "build": "echo 'No build step needed for Node.js server'",
    "start": "bun src/index.js",
    "lint": "eslint src/**/*.js",
    "check-types": "echo 'No TypeScript in server yet'",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio",
    "db:reset": "prisma migrate reset --force",
    "db:seed": "bun run db:seed.js",
    "clean": "rm -rf node_modules prisma/migrations",
    "test": "echo 'No tests configured yet'"
  },
  "dependencies": {
    "@better-auth/cli": "^1.3.6",
    "@hono/swagger-ui": "^0.5.2",
    "@hono/zod-openapi": "^1.1.0",
    "@hono/zod-validator": "^0.7.2",
    "@prisma/client": "^6.14.0",
    "better-auth": "^1.3.6",
    "dotenv": "^17.2.1",
    "hono": "^4.9.1",
    "postgres": "^3.4.7",
    "prisma": "^6.14.0",
    "razorpay": "^2.9.6",
    "stripe": "^18.4.0",
    "zod": "^4.0.17"
  },
  "devDependencies": {
    "@repo/eslint-config": "*",
    "eslint": "^9.33.0"
  }
}
