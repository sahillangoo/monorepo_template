{
  "name": "@repo/typescript-config",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "lint": "echo 'No linting needed for config package'",
    "check-types": "echo 'No types to check for config package'",
    "build": "echo 'No build needed for config package'",
    "clean": "echo 'No cleanup needed for config package'"
  },
  "publishConfig": {
    "access": "public"
  }
}
